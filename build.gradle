plugins {
    id 'java-library'
    id 'eclipse'
    id 'idea'
    id 'maven-publish'
    id 'net.neoforged.moddev' version '2.0.113'
}

version = '1.0.0'
group = 'com.horsemeat'

base {
    archivesName = 'horsemeat'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

neoForge {
    version = '21.1.38'
    runs {
        client {
            client()
            systemProperty 'neoforge.enabledGameTestNamespaces', 'horsemeat'
        }
        server {
            server()
            programArgument '--nogui'
            systemProperty 'neoforge.enabledGameTestNamespaces', 'horsemeat'
        }
    }
    mods {
        horsemeat {
            sourceSet sourceSets.main
        }
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // NeoForge modding dependencies
}

tasks.withType(ProcessResources).configureEach {
    var replaceProperties = [
            minecraft_version: '1.21.1',
            minecraft_version_range: '[1.21.1,1.22)',
            neo_version: '21.1.38',
            neo_version_range: '[21.1.38,)',
            loader_version_range: '[4,)',
            mod_id: 'horsemeat',
            mod_name: 'Horse Meat Mod',
            mod_license: 'All Rights Reserved',
            mod_version: '1.0.0',
            mod_authors: 'Daniel Roberts',
            mod_description: 'Extra meat drops from horses!'
    ]
    inputs.properties replaceProperties

    filesMatching(['META-INF/neoforge.mods.toml']) {
        expand replaceProperties
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}
